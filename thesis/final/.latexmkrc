# Build PDF with pdflatex; use biber for bibliography
$pdf_mode = 1;
$bibtex_use = 2;  # use biber
$clean_ext = 'synctex.gz synctex(busy) run.xml tex.bak bbl bcf fdb_latexmk run tdo %R-blx.bib';

# Optional: uncomment for continuous preview (latexmk -pvc)
# $preview_continuous_mode = 1;
