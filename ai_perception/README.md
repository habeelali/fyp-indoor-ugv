# AI / perception

Vision models, training notebooks, or inference wrappers (e.g. YOLO human detection).

**Suggested migration:** copy relevant folders from `ai_perception/` in your dump, or consolidate only the code you need for the final demo.

Large weights (`.pt`, `.onnx`) are often excluded from git — use Git LFS or document download links in this README.
